# ========================================
# DOCKER COMPOSE ENVIRONMENT CONFIGURATION
# ========================================
# Copy this file to .env and fill in your values
# cp .env.docker .env

# ========================================
# MAIN APP - Coffee Caption Generator
# ========================================

# Ollama Configuration (if using local Ollama)
OLLAMA_URL=http://host.docker.internal:11434
OLLAMA_MODEL=phi3:mini

# Database Configuration (for main app)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=reddit_db
DB_USER=postgres
DB_PASSWORD=postgres123

# ========================================
# OAUTH SERVICE CONFIGURATION
# ========================================

# OAuth Service Database (separate from main app)
OAUTH_DB_HOST=localhost
OAUTH_DB_PORT=5434
OAUTH_DB_NAME=oauth_db
OAUTH_DB_USER=postgres
OAUTH_DB_PASSWORD=oauth_pass

# Service-to-Service Authentication
# IMPORTANT: This must be the same for both services!
OAUTH_SERVICE_API_KEY=your-secure-api-key-here-change-in-production

# Encryption Key (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=your-fernet-encryption-key-here

# Secret Key for sessions
SECRET_KEY=your-secret-key-change-in-production

# ========================================
# INSTAGRAM OAUTH CREDENTIALS
# ========================================
# Get these from: https://developers.facebook.com/

INSTAGRAM_CLIENT_ID=your_instagram_client_id_here
INSTAGRAM_CLIENT_SECRET=your_instagram_client_secret_here
INSTAGRAM_REDIRECT_URI=http://localhost:8001/api/v1/oauth/instagram/callback

# ========================================
# FACEBOOK OAUTH CREDENTIALS
# ========================================
# Get these from: https://developers.facebook.com/

FACEBOOK_CLIENT_ID=your_facebook_client_id_here
FACEBOOK_CLIENT_SECRET=your_facebook_client_secret_here
FACEBOOK_REDIRECT_URI=http://localhost:8001/api/v1/oauth/facebook/callback

# ========================================
# NOTES
# ========================================
# 1. Never commit .env to git (it's in .gitignore)
# 2. Generate strong keys for production
# 3. Keep OAUTH_SERVICE_API_KEY the same in both services
# 4. Encryption key must be a valid Fernet key
# 5. Make sure redirect URIs match what's configured in Meta Developer Portal
